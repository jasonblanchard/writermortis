<div>
    <% if user_signed_in? %>
        <%= link_to "New Story", new_story_path, :class=> 'btn' %></div>
    <% end %>
<h2>Stories</h2>
<%= div_for @stories do |story| %>
    <%= link_to story.title, story %>
<% end %>
