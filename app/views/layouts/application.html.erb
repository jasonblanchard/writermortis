<!DOCTYPE html>
<html>
<head>
  <title>Exquisitecorpse</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= google_webfonts_link_tag 'Rosarivo' %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class='navbar navbar-static-top'>
    <div class='navbar-inner'>
        <%= link_to "Exquisite Corpse", :root, :class => 'brand' %>

        <% if !notice.nil? %>
            <div class='alert alert-success span6 offset1'>
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <%= notice %>
            </div>
        <% end %>
        <% if !alert.nil? %>
            <div class='alert span6 offset1'>
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <%= alert %>
            </div>
        <% end %>

        <ul class='nav pull-right'>
            <% if user_signed_in? %>
                <li><%= link_to "Edit My Account", edit_user_registration_path %></li>
                <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
            <% else %>
                <li><%= link_to "Login", new_user_session_path  %></li>
                <li><%= link_to "Sign Up", new_user_registration_path %></li>
            <% end %>
        </ul>
    </div>
</div>

    <div class='page-wrapper container-fluid'>
        <div class='header'>
            <div class="row-fluid">
            </div>
        </div>

        <%= yield %>
    </div>

</body>
</html>
