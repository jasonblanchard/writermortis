<%= form_for([@story, @story.slices.build]) do |f| %>
    <div>
        <%= f.text_area :body, :rows => 3, :class => 'span8' %>
    </div>
    <%= f.submit "Add to the Story", :class => 'btn' %>
<% end %>
